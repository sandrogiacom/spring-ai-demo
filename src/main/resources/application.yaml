spring:
  application:
    name: spring-ai-demo
  datasource:
    url: jdbc:postgresql://localhost:5432/spring_ai_demo
    username: spring_ai_demo
    password: spring_ai_demo
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
